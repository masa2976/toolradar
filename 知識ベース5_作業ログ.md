# 知識ベース5: トラブルシューティング＆リファレンス

## 🔧 よくあるエラーと対処法

### 1. ModuleNotFoundError: No module named 'wagtail'
```bash
# 原因: パッケージ未インストール
# 対処:
docker compose exec backend pip install wagtail==7.0.6
docker compose restart backend
```

### 2. django.db.utils.ProgrammingError: relation "tools_tool" does not exist
```bash
# 原因: マイグレーション未実行
# 対処:
docker compose exec backend python manage.py makemigrations tools
docker compose exec backend python manage.py migrate tools
```

### 3. CORS エラー（フロントエンド→バックエンド）
```python
# 原因: CORS設定漏れ
# 対処: settings.py に追加
CORS_ALLOWED_ORIGINS = [
    "http://localhost:3000",
]
# または開発時のみ
CORS_ALLOW_ALL_ORIGINS = True
```

### 4. StreamField が管理画面で表示されない
```python
# 原因: use_json_field未設定
# 対処: StreamField定義時に追加
body = StreamField([
    # ブロック定義
], use_json_field=True)  # ← これが必要
```

### 5. Wagtail画像がアップロードできない
```bash
# 原因: メディアディレクトリの権限
# 対処:
docker compose exec backend mkdir -p media/images
docker compose exec backend chmod -R 777 media/
```

### 6. ImportError: cannot import name 'ugettext_lazy'
```python
# 原因: Django 4.0以降でugettext_lazyが削除
# 対処: gettext_lazyを使用
from django.utils.translation import gettext_lazy as _
```

### 7. PostgreSQL接続エラー
```bash
# 原因: DBコンテナが起動していない
# 対処:
docker compose ps  # 状態確認
docker compose up -d db  # DB起動
docker compose logs db  # ログ確認
```

### 8. Redis接続エラー
```python
# 原因: Redis URL設定ミス
# 対処: settings.py確認
CACHES = {
    'default': {
        'BACKEND': 'django_redis.cache.RedisCache',
        'LOCATION': 'redis://redis:6379/0',  # コンテナ名で指定
    }
}
```

### 9. APSchedulerが起動しない
```bash
# 原因: apps.py のready()が実行されていない
# 対処: コンテナを再起動
docker compose restart backend

# ログで確認
docker compose logs backend | Select-String "スケジューラー"

# 期待される出力:
# INFO ... ToolsConfig: スケジューラーを起動しました
# INFO ... スケジューラー: イベントログクリーンアップジョブを登録しました
```

### 10. スケジューラージョブが実行されない
```bash
# 原因1: タイムゾーン設定ミス
# 対処: settings.py確認
TIME_ZONE = 'Asia/Tokyo'
USE_TZ = True

# 原因2: スケジュール設定ミス
# 対処: scheduler.pyのCronTrigger確認
trigger=CronTrigger(day_of_week='sun', hour=3, minute=0)

# 手動実行でテスト
docker compose exec backend python manage.py cleanup_events
```

### 11. ログファイルが生成されない
```bash
# 原因: ログディレクトリが存在しない
# 対処: ディレクトリ作成
docker compose exec backend mkdir -p /app/logs

# または、ホスト側で作成
mkdir -p backend/logs

# 権限確認
docker compose exec backend ls -la /app/logs
```

### 12. Next.js 16でTurbopackが有効化されない
```bash
# 原因: Next.js 16.0.0未満のバージョン
# 対処:
docker compose exec frontend npm list next
# → 16.0.0以降であることを確認

# package.json確認
cat frontend/package.json | Select-String "next"

# 再ビルド
docker compose build frontend --no-cache
docker compose up -d frontend
```

### 13. MCP接続エラー
```bash
# 原因: Claude Desktop設定ミス
# 対処: 設定ファイル確認
# %APPDATA%\Claude\claude_desktop_config.json

# 正しい設定:
{
  "mcpServers": {
    "next-devtools": {
      "command": "npx",
      "args": ["-y", "next-devtools-mcp@latest"]
    }
  }
}

# Claude Desktop再起動
```

### 14. Windows PowerShellでgrepが使えない
```powershell
# 原因: PowerShellではgrepが使えない
# 対処: Select-Stringを使用

# 例: Next.js確認
docker compose logs frontend | Select-String "Next.js"

# 例: エラー確認
docker compose logs backend | Select-String "error" -Context 2
```

## 💡 実装メモ・Tips

### Wagtailページ作成手順
```python
# 1. モデル定義
class BlogPage(Page):
    # フィールド定義
    pass

# 2. マイグレーション
python manage.py makemigrations blog
python manage.py migrate blog

# 3. 管理画面でルートページ作成
# /cms/ にアクセス → Pages → Add child page

# 4. テンプレート作成
# templates/blog/blog_page.html
```

### StreamFieldブロック追加手順
```python
# 1. blocks.py にブロック定義
class CustomBlock(blocks.StructBlock):
    # フィールド定義

    class Meta:
        template = 'blocks/custom_block.html'
        icon = 'placeholder'

# 2. モデルのStreamFieldに追加
body = StreamField([
    ('custom', CustomBlock()),
])

# 3. テンプレート作成
# templates/blocks/custom_block.html
```

### API パフォーマンス改善
```python
# select_related / prefetch_related 使用
queryset = Tool.objects.select_related('stats').prefetch_related('tags')

# only() で必要フィールドのみ取得
queryset = Tool.objects.only('name', 'slug', 'short_description')

# ペジネーション設定
REST_FRAMEWORK = {
    'PAGE_SIZE': 20,
    'DEFAULT_PAGINATION_CLASS': 'rest_framework.pagination.PageNumberPagination',
}
```

### Wagtail画像最適化
```python
# settings.py
WAGTAILIMAGES_FORMAT_CONVERSIONS = {
    'webp': 'webp',
    'jpeg': 'jpeg',
}

# テンプレート
{% load wagtailimages_tags %}
{% image self.featured_image fill-800x450 format-webp %}
```

### キャッシュ戦略
```python
# ビューキャッシュ
from django.views.decorators.cache import cache_page

@cache_page(60 * 15)  # 15分キャッシュ
def tool_list(request):
    pass

# クエリセットキャッシュ
from django.core.cache import cache

tools = cache.get('popular_tools')
if not tools:
    tools = Tool.objects.filter(week_score__gt=100)[:10]
    cache.set('popular_tools', tools, 60 * 60)  # 1時間
```

### APScheduler（自動実行スケジューラー）
```python
# スケジューラーの起動確認
docker compose logs backend | Select-String "スケジューラー"

# 手動でジョブを実行
docker compose exec backend python manage.py cleanup_events

# スケジューラーのステータス確認
docker compose exec backend python manage.py shell
>>> from tools.scheduler import start_scheduler
>>> scheduler = start_scheduler()
>>> print(scheduler.get_jobs())

# ログ確認
docker compose exec backend cat logs/scheduler.log
docker compose exec backend tail -f logs/scheduler.log
```

### APSchedulerのカスタマイズ
```python
# backend/tools/scheduler.py

# 実行時刻を変更（例: 毎日深夜2時）
scheduler.add_job(
    cleanup_old_events,
    trigger=CronTrigger(hour=2, minute=0),  # day_of_weekを削除
    id=job_id,
    name='イベントログクリーンアップ',
    replace_existing=True,
    max_instances=1
)

# 複数のジョブを登録
def update_stats_job():
    call_command('update_weekly_stats')

scheduler.add_job(
    update_stats_job,
    trigger=CronTrigger(day_of_week='sun', hour=4, minute=0),
    id='update_weekly_stats',
    name='週間統計更新',
    replace_existing=True
)
```

### Next.js 16 + Turbopack Tips
```bash
# ビルド速度確認
docker compose logs frontend | Select-String "compiled"
# → Turbopack使用時は "compiled in XXms" と表示

# パフォーマンス計測
docker compose exec frontend npm run build
# → ビルド時間がWebpack比2.6倍高速

# 開発サーバー起動確認
docker compose logs frontend | Select-String "Next.js"
# → "▲ Next.js 16.0.0 (Turbopack)" 表示
```

### MCP活用パターン
```
開発中の典型的なフロー:

1. Next.js開発サーバー起動
   docker compose up -d frontend

2. Claude Desktopでコーディング
   → リアルタイムでエラー検出
   → ハイドレーションエラー自動修正提案

3. ルート確認
   → "どんなルートがあるか教えて"
   → next-devtools-mcpが自動で情報取得

4. 環境変数確認
   → "NEXT_PUBLIC_API_URLの値は？"
   → MCP経由で即座に回答
```

### クリーンなインポート構文（index.ts活用）
```typescript
// Before（個別インポート）
import { Header } from '@/components/layout/Header'
import { BlogFeed } from '@/components/layout/BlogFeed'
import { ToolFeed } from '@/components/layout/ToolFeed'

// After（統一されたインポート）✨
import { Header, BlogFeed, ToolFeed } from '@/components/layout'
```

## 📌 重要リンク

### 開発環境
- Django Admin: http://localhost:8000/admin/
- Wagtail Admin: http://localhost:8000/cms/
- API Docs: http://localhost:8000/api/swagger/
- Frontend: http://localhost:3000/

### ドキュメント
- [Django 5.2 Docs](https://docs.djangoproject.com/en/5.2/)
- [Wagtail 7.0 Docs](https://docs.wagtail.org/en/stable/)
- [Next.js 16 Docs](https://nextjs.org/docs)
- [Next.js MCP Server](https://nextjs.org/docs/app/guides/mcp)
- [Turbopack Docs](https://nextjs.org/docs/app/api-reference/turbopack)

### 参考実装
- Wagtail StreamField: https://github.com/wagtail/wagtail/tree/main/wagtail/blocks
- DRF Best Practices: https://www.django-rest-framework.org/api-guide/
- APScheduler Documentation: https://apscheduler.readthedocs.io/en/stable/
- next-devtools-mcp: https://github.com/vercel/next-devtools-mcp

### プロジェクト内ドキュメント
- [SETUP_WINDOWS_SCHEDULER.md](./SETUP_WINDOWS_SCHEDULER.md) - APSchedulerセットアップガイド
- [SETUP_PHASE1.md](./SETUP_PHASE1.md) - Phase 1実装ガイド

## 🚨 注意事項

### 絶対にやってはいけないこと
1. **本番DBを直接操作**: 必ずバックアップ後
2. **SECRET_KEYをコミット**: .envで管理
3. **DEBUG=Trueで本番運用**: セキュリティリスク
4. **マイグレーションファイル削除**: 履歴が壊れる

### デプロイ前チェック
- [ ] DEBUG=False
- [ ] ALLOWED_HOSTS設定
- [ ] 静的ファイル収集
- [ ] マイグレーション実行
- [ ] キャッシュ設定
- [ ] エラー監視設定

## 📊 パフォーマンス指標メモ

### 現在の数値
- ツール登録数: 0件（テストデータのみ）
- API レスポンス: 平均 150ms
- ページ読み込み: 2.5秒（目標: 2秒以下）
- Next.js ビルド: Turbopack有効化で2.6倍高速化

### ボトルネック
- N+1問題: Tool一覧でstats取得時
- 画像サイズ: 最適化未実施
- キャッシュ: 未設定

## 🔍 調査事項

### 未解決の技術課題
1. Wagtail StreamFieldのA/Bテスト実装方法
2. Next.js ISRとWagtailプレビューの連携
3. 大量ツール登録時のインポート最適化
4. Cache Components（Next.js 16）の実装検討

### 検討中のライブラリ
- django-silk: パフォーマンス分析
- django-cachalot: ORM自動キャッシュ
- wagtail-cache: ページキャッシュ

---

**Note**: プロジェクトの進捗状況はSerenaのメモリ機能で管理されています。作業履歴を確認する際は、Serenaに「進捗を教えて」と尋ねてください。
